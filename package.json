{
  "name": "pulseboard-server",
  "version": "1.0.0",
  "description": "PulseBoard GM Sales dashboard — self-hosted server (Express + Zoho CRM proxy)",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
